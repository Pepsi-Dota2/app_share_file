enum DataStatus {
  initial,
  loading,
  success,
  error,
  empty,
}

enum BottomItem{ 
  home,
  location,
  // cart,
  profile,
}

enum Translate{
  en,
  la
}

enum Status {
  initial,
  loading,
  success,
  userNotFound,
  failure,
}
